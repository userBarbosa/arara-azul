import { BaseLayoutPage } from '../../shared/layouts';

export const Dashboard: React.FC = () => {
  return (
    <BaseLayoutPage title={'Dashboard'} toolbar={<>Barra de Ferramentas</>}>
        Testando
    </BaseLayoutPage>
  );
};
